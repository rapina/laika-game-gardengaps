# GDD — 틈의 정원 / Garden of Gaps

## 잠긴 콘셉트

- 질문: 서로 묶여 따라 움직이는 갈대 사이에 한 마리가 지날 틈을 만들 수 있을까?
- 입력: 장식된 마디를 좌우 드래그하고 놓는다. 장식과 실제 50px 잡기 영역은 일치한다.
- 반응: 선택 갈대 100%, 한 칸 이웃 72%, 두 칸 이웃 35%가 따라간다. 실시간 형상이 예상 틈이다.
- 세계/재료: 물 빠진 밤 습지의 마른 갈대, 얇은 젖은 잎, 무광 딱정벌레. 대표색 청록.
- 종료: 8구간을 통과하면 잎 아래 숨는다. 세 번 막히면 선택 갈대가 굳어 길이 닫힌다.
- 제외: 점수·별·재화·업그레이드·숫자 제한시간·배지·수집품·대사·장식 파티클.

## 한 판

1. 첫 화면은 잡는 곳, 놓는 시점, 8구간 목표와 세 번 막힘을 답한다. 현재 구간의 젖은 청록 잎눈 두 장이 놓아야 할 방향과 허용 폭을 감싸며, 마디가 그 사이에 들어오면 잎맥이 밝아진다.
2. 드래그 중 연결 묶음이 휘고 통로 폭이 즉시 바뀐다.
3. 놓으면 성공 시 다음 구간, 실패 시 해당 갈대가 굳는다.
   첫 실패 뒤에는 현재 구간의 잎눈이 진해지고 상태 문구가 같은 화면에서 다시 찾을 방향과 모양을 짚는다.
4. 행동 상태 `움직여도 돼요 / Shape the gap`과 대기 상태 `지나가는 중 / Let it pass`를 구분한다.
5. 종료 뒤 700ms 보호 후 화면 탭 또는 Enter/Space로 재시작한다.

## 순수 규칙

`src/game/gardenRules.mjs`가 정본이다. 구간 목표 오프셋은 `[-58,46,-34,62,-50,30,-66,54]`, 드래그 범위 ±78, 통로 성공 폭 66 이상이다. 같은 상태·노드·오프셋은 같은 결과를 낸다. 실패한 노드는 `frozen`에 누적된다.

## 화면·소리

390×844 논리 화면을 uniform letterbox하며 backing store는 DPR(최대 3)을 따른다. 갈대는 베지어 관절 변형과 두 겹 섬유선으로 그린다. 생성 키 이미지는 타이틀과 게임 배경 원재료로 쓴다. 소리는 첫 입력 뒤 WebAudio 짧은 마디음/통과음만 낸다. M·숨김·복귀에서 채널을 제어한다.

## 접근성

포인터의 실제 50px 잡기점은 갈대 매듭의 잎눈 장식과 중심이 일치한다. 키보드는 window keydown 좌우 화살표로 가운데 마디 형상을 12씩 조정하고 Enter/Space로 놓는다. 포인터와 키보드 모두 같은 잎눈 허용 폭과 판정을 사용하며, 종료 뒤 Enter/Space는 재시작이다. M 음소거. 한영은 같은 규칙·결과·재시작 정보를 제공한다.
